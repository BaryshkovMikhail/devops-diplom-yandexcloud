apiVersion: v1
kind: Service
metadata:
  name: diploma-test-app
  labels:
    app: diploma-test-app
spec:
  type: ClusterIP
  selector:
    app: diploma-test-app
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
